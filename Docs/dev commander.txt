dev commander

object (with no command) is unselected when saved
? some objects perform differently when run inside the edit window rather than on the taskbar
can't run 2 pCalls in same script

Remove <><> keys and replace with left/right arrows
	Edit save data to allow for "layer" value
Add "visible in list" tickbox
Add ability to add dividers (type "------" or "______")
Add Recent commands option


Update 200
Added "Hide cmd window" option
Added popup window for "are you sure" message when deleting

Fixed exception when moving object down/up too far
Fixed exception when saving with no object selected
Objects with commands are no longer unselected when saved
Fixed "Save Changes" message popping when commands are unchanged